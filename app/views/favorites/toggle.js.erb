$("#favorite-<%= @favorite.location_id %>").html("<%= j render partial: 'favorite_button',
                                                                        locals: { :location => @favorite.location } %>");