<% if next_business %>

    <%= render partial: "businesses/business_location",
               locals: { :business => location_deal.location.business, :location => location_deal.location } %>

    <p><%= link_to 'View All Deals',
                   profile_business_path(location_deal.location.business, { :lid => location_deal.location.id }) %></p>

<% end %>

<%= render partial: "deals/deal_snippet",
           locals: { :deal => location_deal.deal } %>
