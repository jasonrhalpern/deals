$("#favorite-<%= @favorite.location_id %>").html("<%= j render partial: 'shared/favorite_button',
                                                                        locals: { :location => @favorite.location,
                                                                                    :ajax_update => true } %>");